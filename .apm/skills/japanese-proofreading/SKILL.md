---
name: japanese-proofreading
description: >
  人間が読むための日本語(コードコメント・docstring、README・設計文書、BigQuery のカラム説明・スキーマ記述)を、
  Gemini (agy CLI) に添削させて読みやすく直すスキル。エンジニアが手早く書いた圧縮された日本語 —
  助詞が落ちて名詞が数珠つなぎになった文、条件と動作と例外を一文に詰め込んだ文、読点がなく係り受けが読めない文 —
  をほどくのに使う。トリガー — (1) 「添削して」「日本語を直して」「読みやすくして」「文章を整えて」
  「表現を見て」「推敲して」と言われたとき, (2) 特定の文章(コメント、docstring、README、設計文書、カラム説明)を
  指して日本語が硬い・読みにくい・不自然・分かりにくいと指摘されたとき, (3) 「Gemini に添削させて」
  「gemini に見てもらって」と言われたとき。日本語の読みやすさが論点のときに使い、明示的に依頼されたときだけ起動する。
---

# 日本語の添削を Gemini に委譲する

日本語の文章を読みやすく直すよう依頼されたら、この手順に従う。

## 役割分担

**文章を直すのは Gemini、事実を守るのは自分。** この分担が成立の条件になる。

自分が書く日本語は情報を詰め込むほど圧縮され、助詞が落ちて名詞が数珠つなぎになる。自分でその文を読み返しても、
意味が復元できてしまうので読みにくさに気付けない。だから文章の判断は Gemini に渡す。

Gemini には、原文の周りのファイル(コメントが指す関数、カラムの定義)を読ませられる。周りを読めば原文の意味を取り違えにくくなるが、
それでも語調を整える過程で条件が反転したり、限定が落ちたり、周りで読んだ情報を原文に足したり、固有の用語が一般語に置き換わったりする。
それを検知して採否を決めるのは自分の仕事。**Gemini の案は提案であって、反映するかは自分が決める。**

## 手順

### 1. 対象を集める

依頼された範囲の日本語を、自分でファイルから読み取る。**どこからどこまでを直すかは自分が決め、原文は `text` に入れて渡す。**
Gemini に読ませるのは、原文の意味を取るための周りの文脈だけで、書き換えの範囲は渡した原文に限る。

項目の切り方で結果が変わる。

| 対象 | 切り方 |
| --- | --- |
| 文書・節(README、設計文書) | 節をまるごと 1 項目にする。段落のつながりや語の重複まで直せる |
| 散在する短文(コメント、docstring、カラム説明) | 1 箇所 1 項目。ただし**全項目を 1 回の実行にまとめて渡す** |

スクリプトが項目を塊に分けて並行に投げるので、1 件ずつ実行を繰り返すより速く終わる（10 件で 40 秒程度）。

### 2. 依頼の JSON を書く

作業用ディレクトリ(scratchpad など、対象リポジトリの外)に項目の配列を書く。

```json
[
  {
    "id": "token.go:7",
    "kind": "Go の行コメント(関数直上, 1〜2 行)",
    "text": "// 認証トークンの有効期限切れ時に再取得を試行するが、リトライ上限到達時は…",
    "path": "/abs/path/to/repo/auth/token.go",
    "line": 7,
    "hint": "既存表記は『リトライ』(カタカナ)"
  }
]
```

- `id` — 反映先を自分が特定できる名前(`ファイル:行` でも、カラム名でもよい)。重複は不可
- `kind` — **形式と長さの制約を書く**。「BigQuery のカラム説明(全角 60 文字以内)」「README の一文(ですます調)」
  「Python の docstring(1 行)」。ここを空にすると、カラム説明が段落に育つ
- `path` — 任意。原文のあるファイルの**絶対パス**。Gemini はこのファイルを読み、原文の周り
  (コメントが指す関数、同じ文書の前後の節)を確かめてから添削する。`line` で行(`42` や `"42-45"`)を添える
- `refs` — 任意。原文が指すものが別のファイルにあるとき、その**絶対パス**の配列(カラム説明ならテーブル定義や、値を作る SQL)
- `hint` — 任意。ファイルを読んでもわからない文脈を渡す。効果が大きいのは次の 2 つ
  - **プロジェクトの既存表記**(「既存表記は『ユーザ』(長音なし)」) — 指定しないと表記を勝手に統一される
  - **ファイルにない事実**(カラムに実際に入る値の分布、`path` を渡せない対象の前提)

`path` と `refs` は相対パスにしない。agy の読み取りツールは絶対パスしか受け付けず、Gemini は作業ディレクトリを知らない。

### 3. Gemini に投げる

```bash
python3 <この SKILL.md と同じディレクトリ>/scripts/proofread.py --input items.json --output result.json \
  --context-dir /abs/path/to/repo/auth
```

標準出力に、項目ごとの原文・添削文・変更理由が出る。項目は 5 件ずつの塊に分けて並行に投げられる。

- **`--context-dir` には、Gemini に読ませてよいディレクトリを指定する**(繰り返し指定可)。`path` と `refs` は、この中に置く。
  agy はこのディレクトリの中だけ読み取りを許可し、書き込みと、外の読み取りは拒否する
- **`--context-dir` はなるべく狭くする。** 中にある `.env` のような `.gitignore` の対象も Gemini に読まれうるので、
  リポジトリのルートではなく、原文と参考のファイルを含むサブディレクトリを指定する。ホームディレクトリと `/` はスクリプトが拒む。
  秘密を含むファイルと同じディレクトリにしか置けない対象は、`path` を渡さず、周りの文脈を `hint` に書き写す
- `path` も `refs` もない項目だけなら、`--context-dir` は要らない(原文だけで添削する)
- 既定モデルは `gemini-3.8-flash-medium`。原文だけなら短文 10 件で約 40 秒。周りを読ませると、1 回の呼び出しが 20〜30 秒ほどに延びる
- **結果がほとんど「変更なし」で、読み返して納得できないときは `--model gemini-3.8-flash-high` で投げ直す。**
  長文では手を抜いて原文をそのまま返すことがある
- Gemini 側で構造化出力が落ちることが間欠的にあるため、塊ごとに 3 回まで再試行する。
  それでも駄目な塊は `!! 添削できなかった id:` として報告され、**他の塊の結果は残る**。
  失敗した id だけを別の入力ファイルにまとめて投げ直せばよい
- `!!` で始まる行（返ってこなかった id、依頼していない id、agy が拒否した操作）は必ず読む。
  対応づけが崩れたまま反映すると、別の箇所の添削文を書き込むことになる。
  拒否された操作があった塊は、Gemini が必要なファイルを読めていない。`--context-dir` の範囲を見直して投げ直す

### 4. 検証する — このスキルの本体

添削文をそのまま反映しない。**原文と添削文を突き合わせ、変わったのが「言い回しだけ」かを 1 項目ずつ確かめる。**
判断の材料は対象のコード・スキーマ・周辺の文書にある。読まずに採否を決めない。

次に当たるものは却下し、原文を残すか、自分で直した案に差し替える。

| 見るところ | 却下の例 |
| --- | --- |
| 事実の増減 | 原文にない理由・条件・数値が足されている(周りのファイルから持ち込まれたものも含む)。原文にあった但し書きが消えている |
| 限定と条件 | 「5xx のうち **503 のみ**再試行」の「のみ」が落ちる。「A の場合は B」の A の範囲が変わる |
| 独立した事実の接続 | 別々の事実が「〜で、」「〜のため」でつながれ、因果や条件があるように読める |
| 識別子・値 | 関数名・カラム名・型名・数値・単位・URL・列挙値が書き換わる。半角英数が全角になる |
| 用語 | 分野やプロジェクトで定まった語が一般語に置き換わる(「配信」→「配送」など) |
| 表記 | 既存文書と表記が食い違う(ユーザ/ユーザー、サーバ/サーバー) |
| 文体 | である調とですます調が混ざる。体言止めで揃っていた列が「〜です」に変わる |
| 長さ・書式 | カラム説明が上限を超える。コメント記号やインデント、Markdown 記法が崩れる |

ほどけば文は 5〜20% 伸びる。長さの制約がある対象では、伸びた分が制約に収まっているかを実際に数える。

**`note` が「判断できない:」で始まる項目は、Gemini が原文の意味を読み取れなかったもの。**
これは添削の失敗ではなく、原文が読み手に意味を渡せていないという最も強い signal になる。
コードを読んで意味が特定できるなら、自分で書き直した案を出す。特定できないなら、
その項目は原文のまま残し、何が読み取れなかったかをユーザーに提示して聞く。**推測で埋めない。**

**`note` が「食い違い:」で始まる項目は、Gemini が周りを読んで、原文の内容と実装や定義の食い違いに気付いたもの。**
添削文は意味を変えずに直してあるだけなので、食い違いが本当かを自分でコードを読んで確かめ、手順 5 で添削とは別に報告する。

### 5. 反映して報告する

依頼が「見て」「どうか」で止まっていて修正の意図を含まないなら、**検証を済ませた案を提示するところで止める**。
勝手に書き換えない。「直して」「整えて」と言われているなら、採用した案をファイルへ反映する。

どちらの場合も、報告に必ず含める。

- 反映した件数と、変更が大きかった箇所
- **却下した案と、その理由**(「『のみ』が落ちて条件が広がるため原文を残した」)。黙って落とさない
- 「判断できない」で返ってきた項目と、ユーザーへの質問

原文の日本語ではなく**内容そのものが実装と食い違っている**ことに気付いたら、添削の範囲外として別に報告する。
添削で直してよいのは書き方だけで、間違った説明を読みやすく整えても害が増えるだけになる。

## 扱わないもの

- **コメントの要否や有無** — そのコメントを書くべきか、足りないコメントは何かは別の論点(`code-comment-style` の領分)。
  ここで扱うのは、書くと決まった日本語の読みやすさだけ
- **英語の文章** — このスキルの指示文は日本語を前提にしている
- **ユーザーへの回答やコミットメッセージ** — 依頼されれば拒む理由はないが、想定している対象ではない
